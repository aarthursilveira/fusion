services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: postgres://postgres:831042cc9206124cc00d@fusion_db:5432/fusion_core?sslmode=disable
      JWT_SECRET: ${JWT_SECRET}
      NEXTFIT_API_URL: https://integracao.nextfit.com.br
      NEXTFIT_API_TOKEN: ${NEXTFIT_API_TOKEN:-}
      N8N_WEBHOOK_URL: https://n8n.fusion.satomiq.com/webhook/portal
      FRONTEND_URL: https://fusion.satomiq.com
      NODE_ENV: production
      PORT: 3000
